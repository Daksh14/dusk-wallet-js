{
    "tasks": {
        "build": "deno run --allow-read --allow-net --allow-write --allow-env --allow-run build.js",
        "server": "deno run --allow-read --allow-net example/testServer.js",
        "buildServe": "deno task build && deno task server"
    }
}